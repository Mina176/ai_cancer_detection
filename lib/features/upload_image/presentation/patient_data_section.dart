import 'package:flutter/material.dart';

class PatientDataSection extends StatelessWidget {
  const PatientDataSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}