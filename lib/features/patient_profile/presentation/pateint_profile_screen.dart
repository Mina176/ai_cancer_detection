import 'package:flutter/material.dart';

class PateintProfileScreen extends StatelessWidget {
  const PateintProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
